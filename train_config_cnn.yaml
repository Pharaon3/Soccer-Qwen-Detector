# Legacy ResNet18 + temporal CNN training (use run_train_cnn.py)

dataset_root: "dataset"
annotator_dir_name: "annotator"
duration_sec: 30.0
video_fps: 25
num_segments: 30
image_size: 224
val_ratio: 0.15
seed: 42
batch_size: 4
num_workers: 0
epochs: 25
lr: 0.0001
weight_decay: 0.0001
pretrained_backbone: true
mock_video_if_missing: false
max_clips: 0
checkpoint_dir: "checkpoints/cnn"
